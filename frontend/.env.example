# Manufacturing Portal - Environment Configuration

# "backend" (try FastAPI first, fallback to Local) 
# or "local" (Frontend-only, no backend calls)
REACT_APP_DATA_MODE=backend

# Base URL for FastAPI Backend
REACT_APP_API_BASE=http://localhost:8000/api
